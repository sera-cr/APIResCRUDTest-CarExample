{
  "name": "api-crud-car-example-fastify",
  "version": "0.0.1",
  "description": "Example api rest with crud operations using fastify and typescript",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "start": "tsc -w & nodemon --experimental-specifier-resolution=node ./build/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.9",
    "dotenv": "^16.3.1",
    "fastify": "^4.20.0",
    "nodemon": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "typescript": "^5.1.6"
  }
}
